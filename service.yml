apiVersion: v1
kind: Service
metadata:
 name: dotnetworld
spec:
 type: NodePort
 selector:
   app: dotnetworld
 ports:
   - name: client
     protocol: TCP
     port: 80
     nodePort: 32077
 selector:
   app: dotnetworld
